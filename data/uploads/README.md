# Uploads Directory

This folder stores exam files received via the `/pre-atendimento` endpoint. The API writes incoming
uploads here using the `TRIAGE_UPLOAD_DIR` configuration (default `data/uploads`). A `.gitkeep`
placeholder is tracked so the directory exists in version control.
